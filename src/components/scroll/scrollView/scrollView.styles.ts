import { ColorEnum } from '@style/colors.enum';
import styled from 'styled-components';

export const ScrollView = styled.div`
  z-index: 3;
  cursor: pointer;
  border-radius: inherit;
  background-color: ${ColorEnum.GREY};
`;
